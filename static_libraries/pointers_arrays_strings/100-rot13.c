
/**
 * rot13 - crypt a given string
 * using rot13 method
 *
 * @s: the string to crypt
 *
 * Return: s
 */

char *rot13(char *s)
{
	char *p = s;
	char ch[200] ="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";

	while ((*p) != '\0')
	{
		if (((*p) <= 'z' && (*p) >= 'a') || ((*p) <= 'Z' && (*p) >= 'A'))
		{
			while
		}
	}

	return (s);
}
